#pragma once

#include <vector>
#include <list>

using namespace std;

class LeetCode
{
public:
    LeetCode();  
    ~LeetCode();

    static vector<int> twoSum(const vector<int>& nums, int target);
};

